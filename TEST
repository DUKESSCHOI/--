<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>쿠팡 남김해 2W 근무 시작까지 남은 시간</title>
  <style>
    :root {
      --bg-color: #121212;
      --text-color: #f0f0f0;
      --accent-color: #2962ff;
      --danger-color: #ff1744;
      --font-primary: 'Noto Sans KR', sans-serif;
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
      align-items: center;
      background-color: var(--bg-color);
      color: var(--text-color);
      font-family: var(--font-primary);
      padding: 2rem 1rem;
      line-height: 1.5;
    }

    header {
      margin-top: 4rem;
      margin-bottom: 2rem;
      text-align: center;
      max-width: 500px;
      width: 100%;
    }

    .title-line1 {
      font-size: 2.25rem;
      font-weight: 700;
      letter-spacing: 0.05em;
      margin-bottom: 0.25rem;
    }

    .address {
      font-size: 1.2rem;
      color: #999;
      margin-bottom: 0.5rem;
      user-select: none;
    }

    .title-line2 {
      font-size: 1.5rem;
      font-weight: 600;
      color: var(--text-color);
    }

    main {
      flex: 1;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 1rem;
      max-width: 500px;
      width: 100%;
    }

    #alarm-icon {
      font-size: 1.75rem;
      color: var(--danger-color);
      animation: blink 1.2s steps(2, start) infinite;
      user-select: none;
    }

    #timer {
      font-size: 6rem;
      font-weight: 800;
      letter-spacing: 0.02em;
      color: var(--text-color);
      user-select: none;
      transition: color 0.3s ease;
    }

    #timer.blinking {
      color: var(--danger-color);
      animation: blink 1.2s steps(2, start) infinite;
    }

    @keyframes blink {
      0%, 49% { opacity: 1; }
      50%, 100% { opacity: 0; }
    }

    .progress-bar-container {
      width: 100%;
      max-width: 450px;
      height: 16px;
      background-color: #333;
      border-radius: 12px;
      overflow: hidden;
      margin-top: 1.5rem;
      box-shadow: inset 0 0 5px rgba(0,0,0,0.7);
    }

    .progress-bar-fill {
      height: 100%;
      width: 0%;
      background: var(--accent-color);
      border-radius: 12px 0 0 12px;
      transition: width 0.4s ease, background-color 0.4s ease;
    }

    .progress-bar-danger {
      background: var(--danger-color);
    }

    img.logo {
      max-height: 120px;
      border-radius: 12px;
      margin-top: 2rem;
      user-select: none;
      filter: drop-shadow(0 0 4px rgba(0,0,0,0.6));
    }

    .work-message {
      margin-top: 1rem;
      font-size: 1.4rem;
      font-weight: 600;
      color: var(--text-color);
    }

    @media (max-width: 600px) {
      header {
        margin-top: 3rem;
        margin-bottom: 1.5rem;
      }

      .title-line1 {
        font-size: 1.75rem;
      }

      .address {
        font-size: 1rem;
      }

      .title-line2 {
        font-size: 1.25rem;
      }

      #alarm-icon {
        font-size: 1.25rem;
      }

      #timer {
        font-size: 4rem;
      }

      .progress-bar-container {
        max-width: 100%;
        height: 12px;
      }

      .work-message {
        font-size: 1.2rem;
      }
    }
  </style>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR&display=swap" rel="stylesheet" />
</head>
<body>
  <header>
    <div class="title-line1">남김해1캠프 소분3(2W)</div>
    <div class="address">근무지 주소 : 김해시 안동 163-2 7층</div>
    <div class="title-line2"><span id="work-date"></span> 근무 시작까지 남은 시간</div>
  </header>

  <main>
    <div id="alarm-icon">⏰</div>
    <div id="timer">00시간 00분</div>

    <div class="progress-bar-container" aria-label="근무 시작까지 남은 시간 진행률">
      <div class="progress-bar-fill" id="progress-bar"></div>
    </div>

    <img src="후보2.png" alt="회사 로고" class="logo" />
    <div class="work-message">근무 잘 부탁드립니다!</div>
  </main>

  <script>
    function formatDate(date) {
      const day = date.getDate().toString().padStart(2, '0');
      return `${day}일`;
    }

    function updateTimer() {
      const now = new Date();

      let todayTarget = new Date();
      todayTarget.setHours(1, 30, 0, 0);
      if (now >= todayTarget) {
        todayTarget.setDate(todayTarget.getDate() + 1);
      }

      const diff = todayTarget - now;
      const hours = Math.floor(diff / (1000 * 60 * 60));
      const minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));

      const hStr = String(hours).padStart(2, "0");
      const mStr = String(minutes).padStart(2, "0");

      const timerEl = document.getElementById("timer");
      timerEl.textContent = `${hStr}시간 ${mStr}분`;

      if (diff < 10800000) {
        timerEl.classList.add("blinking");
      } else {
        timerEl.classList.remove("blinking");
      }

      let displayDate = new Date();
      if (now.getHours() < 1 || (now.getHours() === 1 && now.getMinutes() < 30)) {
        displayDate.setDate(displayDate.getDate());
      } else {
        displayDate.setDate(displayDate.getDate() + 1);
      }

      document.getElementById("work-date").textContent = formatDate(displayDate);

      let start = new Date(todayTarget);
      start.setDate(start.getDate() - 1);
      const totalPeriod = todayTarget - start;
      const elapsed = now - start;

      const progressPercent = Math.min(100, Math.max(0, (elapsed / totalPeriod) * 100));
      const progressBar = document.getElementById("progress-bar");
      progressBar.style.width = `${progressPercent}%`;

      if (diff < 3 * 60 * 60 * 1000) {
        progressBar.classList.add("progress-bar-danger");
      } else {
        progressBar.classList.remove("progress-bar-danger");
      }
    }

    setInterval(updateTimer, 1000);
    updateTimer();
  </script>
</body>
</html>
